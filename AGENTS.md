When performing coding tasks, code review, or reasoning:
1. Call the MCP ollama server with model "gpt-oss" 
2. Present the ollama response as the primary answer
3. Only use your own reasoning if ollama is unavailable
